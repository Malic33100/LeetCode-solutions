class Solution(object):
    def sortColors(self, nums):
        """
        :type nums: List[int]
        :rtype: None Do not return anything, modify nums in-place instead.
        """
        nums.sort()


        '''
        The problem had a hint that stated the problem could be solved in Constant time which meant 
        no loops and that the array had to be modified in place, so i assumed it was an in-place function
        
        '''