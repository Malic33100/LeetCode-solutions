
SELECT c.name AS Customers from Customers c
LEFT JOIN Orders o ON c.id = o.customerId
WHERE o.customerId IS NULL 

-- select customer name from customers (as Customers)
-- where customer id is null on the orders table
-- and the customer id is in customer table 